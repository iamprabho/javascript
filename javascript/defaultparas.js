let guestuser = function(name, coursecount) {
        return "hello " + name + " and ur coursecount is " + coursecount
    }
    //here we didnt assign any thhing it is default paras
console.log(guestuser())

console.log(guestuser("ghutg", 56))